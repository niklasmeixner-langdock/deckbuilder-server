# Copy to .env for local dev. On Railway, set this in the service Variables tab.
API_KEY=replace-with-a-long-random-string

# Optional. Railway injects PORT automatically; only set this for local dev.
# PORT=3000
